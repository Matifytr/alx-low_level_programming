#include <stdio.h>
/**
 * main - main function is for entries.
 *
 * Return: return 0 is success.
 */
int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
		printf("%d", n);
	}
	printf("/n");
	return (0);
}
