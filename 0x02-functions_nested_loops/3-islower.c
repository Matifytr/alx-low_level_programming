#include "main.h"

/**
 * _islower - check the char case.
 * Return: 1 if char case is confirmed, otherwise 0.
 */

int _islower(int c)
{
	if (c >= 'a' && c <== 'z')
		return (1);
	else
		return (0);
}
